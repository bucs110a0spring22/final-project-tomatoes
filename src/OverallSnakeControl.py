class OverallSnakeControl:
  def assembleParts:
    # Assemble the snake parts (head, body (straight & bent), tail)
  
  def userInput:
    # Detects and takes user input and generate the desired coordinates of the input
     
  def partsLocationControl:
    # Controls the coordinates of boody parts when they're moved using the input
    
  def partsDirectionControl:
    # Controls the heading of body parts when they're moved using the input
  
  def statusChangeControl:
    # Changes the image file when snake gets longer/shorter & the status of the heading (eating, died, normal, etc)