class Screen:
  def startScreen:
    # Genarates a start bottom in the middle and start the game when user press it.
  
  def finalScreenScores:
    # Display the total emotion eaten
     
  def finalScreenHighest:
    # Keep track of the history highest score (only saves the highest) & compare with the curren score
    
  def finalScreenWords:
    # Displays congrats if history > curren; displays try again if not