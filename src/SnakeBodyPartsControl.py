class SnakeBodyPartsControl:
  def position:
    # Updates & saves the position/coordinantes of each body part once they are moved.
  
  def direction:
    # Updates & saves the heading/direction of each body parts once they are moved.